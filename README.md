# 自定义nextcloud docker镜像

给`nextcloud:*fpm`镜像添加`ffmpeg`和`ffmpegthumbnailer`软件包

## 使用

```bash
docker pull ghcr.io/tablebird/nextcloud:latest
## or
docker pull ghcr.io/tablebird/nextcloud:版本号-fpm
```

最新版本号见[.last_version](./.last_version)
